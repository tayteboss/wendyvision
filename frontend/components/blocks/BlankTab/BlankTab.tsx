import styled from "styled-components";

const BlankTabWrapper = styled.section`
  background: var(--colour-white);
  min-height: 100vh;
`;

const BlankTab = () => {
  return <BlankTabWrapper></BlankTabWrapper>;
};

export default BlankTab;
